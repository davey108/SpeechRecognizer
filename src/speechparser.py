import speech_recognition as sr
from pydub import AudioSegment
import typing

"""
Module to transform an audio file with sounds to text string using SpeechRecognition package and 
its two classes: Recognizer and AudioFile.
"""
class SpeechToText:
    def __init__(self, audio_file: str):
        """
        Constructor of SpeechToText, accepting the path to the audio file in mp3 format
        @params audio_file - the path to the mp3 audio file
        """
        
        
    
